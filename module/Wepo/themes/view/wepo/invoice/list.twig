{% extends 'partial/list.twig' %}

{% set title = translate('Invoices') %}

{% block leftlink %}
        {#<div id="titleSearch" class="titleSearch" style="">#}
                        {#<form class="search_form" action="{{ url( null, params|removeKey([ 'q', 'p']) )~saurl }}"#}
                              {#method="get" name="list_search">#}

                            {#<input type="text" name="q" value="{{ search_query }}"#}
                                   {#placeholder="Search in &quot;{{ title }}&quot;"/>#}

                            {#<div class="searchico"></div>#}

                            {#{% if search_query %}#}
                                {#<span style="font-style: italic; color: #999999; "> (#}
                                    {#{% if  paginator.getTotalItemCount == 0 %}#}
                                        {#nothing#}
                                    {#{% elseif paginator.getTotalItemCount == 1 %}#}
                                        {#{{ paginator.getTotalItemCount }} match#}
                                    {#{% else %}#}
                                        {#{{ paginator.getTotalItemCount }} matches#}
                                    {#{% endif %}#}
                                    {#)</span>#}
                            {#{% endif %}#}

                        {#</form>#}
                    {#</div>#}
{% endblock leftlink %}
{% block rightlink %}
    <a href="{{ url( 'setup', { 'action': 'view', 'table':table.id } )~saurl }}">
                    {{ translate('Output fields') }}
                </a>
{% endblock rightlink %}

{% block data %}
    {% include 'partial/invoice-list.twig' %}
{% endblock data %}

{% block action %}
    <a class="actionview" href="{{ url( 'invoice', { 'action': 'view','id':avalue.id() } )~saurl }}"><img src="/img/1.png" class="view"  alt="{{translate('View')}}" /><span class="tooltip top">View</span></a>&nbsp;
{#    <a class="actionview" href="{{ url( 'invoice', { 'action': 'delete','id':avalue.id() } )~saurl }}"><img src="/img/1.png" class="delete"  alt="{{translate('Delete')}}" /><span class="tooltip top">Delete</span></a>&nbsp;#}
    <a class="actionview" href="{{ url( 'invoice', { 'action': 'convert','id':avalue.id() } )~saurl }}"><img src="/img/1.png" class="convert"  alt="{{translate('Payment')}}" /><span class="tooltip top">Convert to&nbsp;payment</span></a>
    <a class="actionview"><img src="/img/1.png" class="delete single_del"  alt="{{translate('Delete')}}" /><span class="tooltip top">Delete</span></a>&nbsp;
{% endblock action %}
